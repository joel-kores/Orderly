info:
  contact: {}
paths:
  /health:
    get:
      consumes:
      - application/json
      description: Get health status of app
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get health status of app
      tags:
      - health
swagger: "2.0"
